name: Build 200responder

on:
  push:
    branches: [ "master" ]
    paths:
      - apps/200responder/**
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:
    inputs:
      reason: 
        description: 'Reason'
        required: true
        default: 'Manual trigger'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./apps/200responder
    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --tag 200responder:latest
